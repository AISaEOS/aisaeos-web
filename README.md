# aisaeos-web

Official website of AISaEOS Oy — V001.

Stack: Astro 5.x, TypeScript (strict), CSS custom properties.
No runtime dependencies, no framework, no CMS.

Planning documents live in the internal Claude project, not in this repo.

## Local development
npm install
npm run dev
